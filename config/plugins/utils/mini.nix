{
  plugins.mini = {
    enable = true;
    modules = {
      ai = { };
      icons = { };
      notify = {
        lsp_progress = {
          enable = true;
          duration_last = 1000;
        };
      };
      pairs = { };
      surround = { };
      tabline = { };
      trailspace = { };
    };
  };
}
